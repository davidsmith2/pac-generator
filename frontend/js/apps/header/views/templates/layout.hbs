<div class="col-sm-4">
    <h1 class="mt0">{{title}}</h1>
</div>
<div class="col-sm-4">
    <p class="mt1 text-center"><small>Signed in as <strong>{{username}}</strong>. <a href="/logout">Sign out</a>.</small></p>
</div>
<div class="col-sm-4">
    <button class="btn btn-default js-publish pull-right" disabled="true" type="button">
        <span class="glyphicon glyphicon-save"></span>
        Publish PAC files
    </button>
</div>
